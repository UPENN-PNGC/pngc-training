[project]
name = "ai-assisted-coding-seminar"
version = "0.1.0"
description = "Example project for AI-assisted coding and Co-pilot Workflows seminar."
authors = [
    { name = "PNGC" },
]
requires-python = ">=3.8"
dependencies = [
    "pytest",
]
